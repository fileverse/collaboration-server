# Server Configuration
PORT=5001
HOST=0.0.0.0
NODE_ENV=development

# CORS Configuration (comma-separated origins, or * to allow any origin)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Authentication
SERVER_DID=did:key:z6MkhaXgBZDvotDkL5257faiztiGiC2QtKLGpbnnEGta2doK

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
# Collab wire format rollout: unset (ECIES everywhere, never locks) or "xchacha"
# (announce XChaCha and lock a document once every socket in its room can read it).
# WIRE_FORMAT_TARGET=xchacha
